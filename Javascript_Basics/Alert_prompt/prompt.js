let num = prompt("Enter a number");

if (num % 3 === 0) {
    document.write(num + " is a multiple of 3");
} else {
    document.write(num + " is not a multiple of 3");
}

// this will directly print the result on web page